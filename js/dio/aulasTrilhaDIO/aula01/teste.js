var ias = 0; 
var mim = 1;
/* Comentários 
// com
// várias
// linhas. */
// CTRL + / + SELEÇÃO DO QUE É DESEJADO = Tudo estará selecionado.

var preco = 2;
var desconto = 0.2;

const constante = 10;

preco = preco - desconto;

function soma(a, b) {
    console.log(a+b);
    return a + b;
}

soma(10 + 9); // As funções seguem amesma lógica que Java.
